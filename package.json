{
  "name": "hammedialabs",
  "private": true,
  "version": "0.1.0",
  "description": "ProjectHydra — HamMediaLabs operating system (HQ + brand factory).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test core/**/*.test.ts",
    "test:scoring": "tsx --test core/scoring/scoring.test.ts",
    "test:router": "tsx --test core/router/router.test.ts",
    "heartbeat": "tsx core/jobs/heartbeat.ts",
    "hml": "tsx core/cli/hml.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
